# =============================================================
# Beer_eisagogi_dedomenon.R
# Εισαγωγή δεδομένων πωλήσεων μπίρας (2016-2019)
# =============================================================

# Πωλήσεις μπίρας (εκατομμύρια μπουκάλια) ανά εποχή
beer_values <- c(
  1, 3, 6, 4,  # 2016: Χειμώνας, Άνοιξη, Καλοκαίρι, Φθινόπωρο
  2, 2, 7, 5,  # 2017
  2, 4, 8, 5,  # 2018
  1, 3, 8, 6   # 2019
)

# Δημιουργία χρονοσειράς (τριμηνιαία, frequency=4)
beer_ts <- ts(beer_values, start = c(2016, 1), frequency = 4)

message(">>> Τα δεδομένα πωλήσεων μπίρας φορτώθηκαν επιτυχώς!")